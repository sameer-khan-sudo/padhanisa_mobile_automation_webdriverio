"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SessionDebugMap = void 0;
class SessionDebugMap {
    constructor() {
        this.sessionMap = new Map();
    }
    createNewSession(sessionId) {
        if (!this.sessionMap.get(sessionId)) {
            this.sessionMap.set(sessionId, { is_paused: false });
        }
    }
    get(sessionId) {
        return this.sessionMap.get(sessionId);
    }
    set(sessionId, data) {
        let session = this.get(sessionId);
        if (session) {
            this.sessionMap.set(sessionId, Object.assign(Object.assign({}, session), data));
        }
    }
}
exports.SessionDebugMap = SessionDebugMap;
exports.default = new SessionDebugMap();
