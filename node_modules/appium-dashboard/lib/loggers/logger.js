"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logger = void 0;
const config_1 = require("../config");
const winston = require("winston");
const logger = winston.createLogger({
    format: winston.format.combine(winston.format.timestamp(), winston.format.simple(), winston.format.printf(({ level, message, timestamp, label }) => {
        return `${level.toUpperCase()} : ${timestamp}  ${message} `;
    })),
    transports: [new winston.transports.File({ filename: config_1.config.logFilePath })],
});
exports.logger = logger;
